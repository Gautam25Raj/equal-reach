import PageHeader from '@/components/layout/PageHeader';

const Notification = () => {
  return (
    <div>
      <PageHeader title="Notifications" isRefresh={true} opacity />
    </div>
  );
};
export default Notification;
